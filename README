A simple Bitbucket plugin to require that all developers submit a pull request to get their code to the develop branch.

This allows all developers to be approvers on pull requests and merge them but not able to push directly.
